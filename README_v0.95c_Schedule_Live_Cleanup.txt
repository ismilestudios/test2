iSmile Scheduler v0.95c — Schedule Live cleanup

Replace these files:
- components/SchedulerApp.jsx
- package.json

Changes:
- Schedule Live is launched from a prominent Overview button instead of appearing as a normal top navigation tab.
- Schedule Live workspace uses a wider/full-screen layout.
- Visual theme shifted from black/red to deep iSmile green with the red Live accent preserved.
- Weekly Rollouts and per-photographer rollout counts are merged into the top command area.
- Removed the large left Draft Board column to give event cards more room.
- Event cards are more compact and less vertically stretched.
- Overview button is more prominent and explains the live scheduling room.

Commit message:
Refine Schedule Live layout v0.95c
